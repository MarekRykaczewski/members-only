extends layout

block content
    p(class="text-xl font-bold") Do you really want to delete this Message?

    form(method='POST' action='')
        div
        input#messageid(type='hidden',name='messageid', required='true', value=message._id )

        button(class="mt-5 border border-red-700 bg-red-200 hover:bg-red-100 transition px-3 py-1" type='submit') Delete
